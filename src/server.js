const config=require('./config');const app=require('./app');const {ensureDemo}=require('./services/bootstrapService');let server;
async function shutdown(signal){console.log(JSON.stringify({level:'info',event:'server_stopping',signal}));if(server)server.close(async()=>{if(config.dbDriver==='mysql')await require('./database/mysql').close();process.exit(0)});else process.exit(0)}
async function main(){try{if(config.dbDriver==='mysql')await require('./database/mysql').health();await ensureDemo();server=app.listen(config.port,()=>console.log(JSON.stringify({level:'info',event:'server_started',port:config.port,environment:config.nodeEnv,database:config.dbDriver})))}catch(error){console.error(JSON.stringify({level:'error',event:'server_start_failed',message:error.message}));process.exit(1)}}process.on('SIGINT',()=>shutdown('SIGINT'));process.on('SIGTERM',()=>shutdown('SIGTERM'));main();