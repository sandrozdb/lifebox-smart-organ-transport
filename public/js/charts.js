window.LifeBoxCharts={draw(canvas,values,color='#1ed6c5'){const ctx=canvas.getContext('2d'),ratio=devicePixelRatio||1,w=canvas.clientWidth,h=145;canvas.width=w*ratio;canvas.height=h*ratio;ctx.scale(ratio,ratio);ctx.clearRect(0,0,w,h);ctx.strokeStyle='#1a3a50';ctx.lineWidth=1;for(let i=1;i<4;i++){const y=i*h/4;ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke()}if(!values.length)return;const min=Math.min(...values),max=Math.max(...values),range=max-min||1,pad=8;ctx.beginPath();values.forEach((v,i)=>{const x=pad+i*(w-pad*2)/Math.max(values.length-1,1),y=h-pad-(v-min)/range*(h-pad*2);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.strokeStyle=color;ctx.lineWidth=2.5;ctx.lineJoin='round';ctx.stroke();ctx.fillStyle='#7e9cb1';ctx.font='10px Segoe UI';ctx.fillText(max.toFixed(1),2,11);ctx.fillText(min.toFixed(1),2,h-2)}};

